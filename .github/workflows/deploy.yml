# Builds the prerendered site and publishes dist/Recipio/browser to GitHub Pages
# at https://ngagua.github.io/Recipio/ on every push to main.
# One-time setup: repo Settings → Pages → Build and deployment → Source: "GitHub Actions".
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npx ng test --watch=false
      - run: npx ng build --base-href /Recipio/
      # Unknown paths get the client-side app instead of GitHub's 404 page
      - run: cp dist/Recipio/browser/index.csr.html dist/Recipio/browser/404.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist/Recipio/browser

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
